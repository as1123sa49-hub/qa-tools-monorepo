import { getTodayStr } from './case-utils.js';

export const PROMPT_HIERARCHY_RULES = `
### 階層欄位（必填）
- "主模組"：對照表「主模組」。例：通用功能、會員系統、金流系統、KYC審核、遊戲大廳。
  - 規格寫「玩家系統」時，主模組填「會員系統」。
- "層級"：僅填 **L1**、**L2** 或 **L3**（英文）。
- **L2 vs L3（必守）**：
  - 若「功能頁面/元件」與規格書 ◆ 標題**同名或同一段落**（如「玩家註冊流程規格」「存款功能規格」），且規格**未再細分**下級章節，層級必須填 **L2**。
  - 僅當驗證**明確細項**（如「服務條款內容規格」「音量控制規格」，且規格在 L2 功能群之下還有子項）時填 **L3**。
  - **禁止**把所有案例的層級都填成 L3；會員系統、金流、遊戲大廳等 MVP 摘要多為 L2。
- "功能頁面/元件"：填 **「L2 功能群 - 具體項目」**（半形連字號 - 分隔），供平台樹狀圖分層。
  - 例：玩家查詢 - 批量存提查詢、玩家儀表板 - 人員備註、存款功能 - 限額驗證
  - L2 功能群對照規格 ◆ 標題或章節（如玩家查詢、玩家儀表板）；具體項目為該章節下驗證的頁面/欄位/流程。
  - 僅當規格確實無上層章節時，才可只寫單一名稱；勿用斜線路徑（勿寫「法律與合規 / 服務條款」）。
  - 差異比對在末尾加 [新增] 或 [變更]；無章節可對照時加 [AI自創]。

### 類別與優先度
- 通用功能、會員系統、金流系統、KYC審核 → 類別填 **平台**；遊戲大廳 → **大廳**；房內/後台/異常依規格。
- "優先度"：[P0, P1, P2]，勿加括號說明。

### 輸出陣列排序
1. 依規格書 ◆ 標題順序：主模組 → L2 區塊內案例 → 下一 L2…
2. 同一節點內：正面→負面→邊界，再 P0→P1→P2。
`;

export const PROMPT_SPEC_SOURCE_RULES = `
### 規格來源（精確溯源，必守）
- PDF / DOCX：**規格書檔名** / 頁碼或章節（例：玩家管理PRD.docx / 第 4 頁、玩家管理PRD.docx / 5.2. 玩家查詢）
- **XLSX / CSV 表格**：**規格書檔名** / **工作表名稱** / 列號（例：官網規格書.xlsx / 公司介紹 / 列 34）
  - 工作表名稱必須取自內容中的【工作表：…】標題，**不可省略**（即使只有一個工作表）
- 嚴禁填寫指令編號（如 1.a）`;

function promptJsonKeysFull() {
  return `
- 每個物件必須嚴格包含以下鍵值（請依此順序排列）：
    - "測試類型": [正面測試, 負面測試, 邊界測試]
    - "類別": [平台, 大廳, 房內, 異常處理, 後台] 等
    - "優先度": [P0, P1, P2]
    - "主模組": 見上
    - "層級": [L1, L2, L3]
    - "功能頁面/元件": 見上
    - "前置條件": 若無則填「無」
    - "測試標題": 必填
    - "預期結果": 必填（30字內）
    - "規格來源": 見「規格來源」規則；XLSX 必含工作表名（例：官網規格書.xlsx / 公司介紹 / 列 34）
    - "編號": 類型縮寫_類別縮寫_序號（如 POS_PLT_001）
    - "版本標籤": "${getTodayStr()}_v1"
`;
}

export const PROMPT_JSON_KEYS_BASELINE = `
- 每個物件必須包含下列欄位（不可省略，請依此順序）：
  "狀態","取代者","編號","測試類型","類別","優先度","主模組","層級","功能頁面/元件","前置條件","測試標題","預期結果","規格來源","版本標籤"
`;

export const PROMPT_FULL = (specText) => `你是一位擁有 10 年經驗的資深遊戲 QA 測試專家。請針對下方【規格書內容】進行「深度邏輯掃描」，並產出結構化的測試案例。

### 核心操作原則：
1. **【排除刪除線】**：嚴禁針對有 [刪除線] 或 [中劃線] 的廢棄規則設計案例。
2. **【原子化拆解】**：嚴禁將多個驗證點寫在同一條案例中。請將每個動作與預期結果拆解到最小單位，以極大化案例數量。
3. **【精確溯源】**：「規格來源」必須 100% 可追溯（見下方規格來源規則）。
4. **【章節對應】**：依規格書 ◆ 標題判斷主模組與功能頁面/元件，並依章節順序輸出案例。
5. **【多文件優先級】**：若內容含「主規格文件」與「補充文件」，主規格只用來決定主模組/層級，補充文件只用來補流程細節；兩者衝突時以主規格的模組與層級定義為準。
${PROMPT_SPEC_SOURCE_RULES}

### 測試案例產出指令：
1. **全路徑與邏輯窮舉**：正面流程、負面與異常、邊界測試、UI/特效等。
${PROMPT_HIERARCHY_RULES}

### 輸出規範 (JSON 格式)：
- 僅回傳純 JSON 陣列，不包含 Markdown 標記，不包含說明文字。
- **每個欄位皆為必填，絕對不可省略 key 或留空字串。若無適當內容則填「無」或「—」。**
${promptJsonKeysFull()}

### 參考來源：
${specText}`;

export const PROMPT_MULTI = `你是一位擁有 10 年經驗的資深遊戲 QA 測試專家。請針對下方【規格書內容】進行「深度邏輯掃描」，並產出結構化的測試案例。

### 本批掃描範圍
- **模組索引／分類**：{{INDEX_NAME}}（僅用於主模組、層級與功能分類，非案例內容來源）
- **當前功能規格書**：{{CURRENT_PRD}}（**本批必須完整邏輯窮舉掃描**）
- **全部功能規格書清單**：{{SUPPLEMENT_LIST}}

### 分類規則（必守）
1. **【索引優先】**：「主模組」「功能頁面/元件」必須**優先**對照【模組索引／分類】中的模組名稱與功能群（如玩家查詢、玩家儀表板、玩家帳號看板）。
   - 填寫格式：**L2功能群 - 具體項目**（例：玩家查詢 - 批量存提查詢）
2. **【章節 fallback】**：若索引中**找不到**對應分類，再依功能規格書的 ◆ 標題或章節編號自行分類（與全量產出模式相同）。
3. **【內容來源】**：測試規則必須來自「當前功能規格書」全文，嚴禁只掃索引摘要。

### 已產出規則（勿重複相同驗證點）
{{EXISTING_RULES}}

### 核心操作原則：
1. **【排除刪除線】**：嚴禁針對有 [刪除線] 或 [中劃線] 的廢棄規則設計案例。
2. **【原子化拆解】**：嚴禁將多個驗證點寫在同一條案例中。請拆解到最小單位，**極大化案例數量**。
3. **【精確溯源】**：「規格來源」必須填**當前功能規格書檔名**（或簡稱）+ 該份規格書**原文**的頁碼、章節或工作表/列號（XLSX 必含工作表名）；**勿**改寫成模組索引的章節名稱。
4. **【勿重複驗證點】**：若「已產出規則」已含相同功能+相同驗證類型，勿再產出語意相同的案例。
${PROMPT_SPEC_SOURCE_RULES}
${PROMPT_HIERARCHY_RULES}

### 測試案例產出指令：
1. **全路徑與邏輯窮舉**：正面流程、負面與異常、邊界測試、UI/特效等。

### 輸出規範 (JSON 格式)：
- 僅回傳純 JSON 陣列，不包含 Markdown 標記，不包含說明文字。
- **每個欄位皆為必填，絕對不可省略 key 或留空字串。若無適當內容則填「無」或「—」。**
${promptJsonKeysFull()}

### 參考來源：
{{SPEC}}`;

export const PROMPT_DIFF = (newSpecText, oldSpecText) => `你是一位擁有 10 年經驗的資深遊戲 QA 測試專家。請針對下方提供的【舊版規格】與【新版規格】進行「差異增量掃描」，並僅針對「變動或新增」的邏輯產出結構化測試案例。

### 數據來源：
- 【舊版規格內容(比對基準)】：${oldSpecText}
- 【新版規格內容(本次更新)】：${newSpecText}

### 增量比對原則：
1. **僅針對差異產出**：對比兩份規格，若功能點、數值、描述完全無變動，則不產出案例。
2. **追蹤變更**：在「功能頁面/元件」末尾加註 [新增] 或 [變更]。

### 核心操作原則（嚴格遵守）：
1. **【排除刪除線】**：嚴禁針對有 [刪除線] 或 [中劃線] 的廢棄規則設計案例。
2. **【原子化拆解】**：每個物件僅包含單一驗證點。
3. **【精確溯源】**：「規格來源」必須填**新版規格書檔名**（見【規格書：檔名】標題）+ 頁碼、章節或工作表/列號。
${PROMPT_SPEC_SOURCE_RULES}
${PROMPT_HIERARCHY_RULES}

### 輸出規範 (JSON 格式)：
- 僅回傳純 JSON 陣列，不包含 Markdown 標記，不包含說明文字。
- **每個欄位皆為必填，絕對不可省略 key 或留空字串。若無適當內容則填「無」或「—」。**
${promptJsonKeysFull()}`;

export const PROMPT_BASELINE_DIFF = (newSpecText, baselineCasesJson) => `你是一位資深遊戲 QA 測試專家。請根據【新版規格書】與【既有 TestCase 清單】做覆蓋比對，輸出可直接執行的測試案例結果。

【新版規格書】：
${newSpecText}

【既有 TestCase 清單】：
${baselineCasesJson}

### 任務要求
1. 保留仍然有效的案例（狀態填「有效」）。
2. 對已不適用的案例標記為「失效」，並盡量在「取代者」填入新版對應案例編號，若無則填空字串。
3. 對規則或數值改動造成需重寫的案例，輸出新版可執行案例，狀態填「有效」。
4. 對新版新增功能，補上新案例，狀態填「有效」。
5. 以「編號」作為識別。若無法沿用舊編號，可產生新編號。
${PROMPT_SPEC_SOURCE_RULES}
${PROMPT_HIERARCHY_RULES}

### 輸出規範（僅回傳 JSON 陣列）
${PROMPT_JSON_KEYS_BASELINE}
- 若某欄無內容，請填「—」。
- 僅允許上述欄位，禁止輸出其他 key。
- 不要輸出 Markdown，不要輸出說明文字。`;

export const PROMPT_OBSOLETE = (newSpecText, caseSummaryJson, newCaseSummaryJson) =>
`你是一位資深遊戲 QA 測試專家。請根據【新版規格書】審查【現有測試案例清單】，找出哪些案例已不符合新版規格（功能已移除、規則或數值已更改、條件已不適用等）。
同時，若某個失效案例在【新增案例清單】中有功能相近的新案例，請填入其編號作為「取代者」。

【新版規格書】：
${newSpecText}

【現有測試案例清單（JSON）】：
${caseSummaryJson}

【新增案例清單（JSON）】：
${newCaseSummaryJson}

### 輸出規範：
- 僅回傳 JSON 物件陣列，每個物件包含：
  - "編號": 失效的舊案例編號（字串）
  - "取代者": 新增案例中功能相近的編號（字串），若無對應則填 null
- 若所有案例仍有效，回傳空陣列：[]
- 範例：[{"編號":"NEG_ROOM_001","取代者":"NEG_ROOM_012"},{"編號":"POS_LOB_003","取代者":null}]
- 不包含 Markdown 標記，不包含任何說明文字`;

export const PROMPT_DUP_CONFIRM = (groupsJson) => `你是資深 QA。以下為同一「功能頁面/元件 + 規則族」的疑似重複案例群組。
請判斷是否為「同一驗證點的重複描述」（應標記為重複），還是「同規則但不同流程步驟」（不應視為重複，如存款 vs 提款）。

僅回傳 JSON 陣列：
{ "groupId": "DUP-001", "isDuplicate": true|false, "reason": "簡短說明" }

群組資料：
${groupsJson}`;

export const PROMPT_MODULAR_CONFIRM = (clustersJson) => `你是資深 QA。以下為多批測試案例中「疑似相同」的群組（程式依測試類型+規則族初步分群）。
請判斷每個群組是否為「同一驗證規則、可收成一個共用模組」。入口跳轉類與頁內編輯/欄位驗證類不可併為同一模組。

僅回傳 JSON 陣列，每項格式：
{ "clusterKey": "...", "sameModule": true|false, "reason": "簡短說明" }

群組資料：
${clustersJson}`;

export const PROMPT_REFILL = (incompleteCasesJson, specText) =>
`你是一位資深遊戲 QA 測試專家。以下是部分欄位為空的測試案例，請根據【規格書內容】補全缺少的欄位。

【需補全的案例（JSON）】：
${incompleteCasesJson}

【規格書內容】：
${specText}

### 輸出規範：
- 回傳完整的 JSON 陣列，每個物件保留原有「編號」不變
- 僅補全空白欄位，非空欄位原樣保留
- 每個欄位皆為必填，根據測試標題和規格內容合理推斷；若實在無法判斷，填「—」
- "主模組"、"層級"(L1/L2/L3)、"功能頁面/元件"：依規格階層填寫，不可空白；"功能頁面/元件" 請用「L2功能群 - 具體項目」格式（如 玩家儀表板 - 人員備註）
- "規格來源"：PDF/DOCX 填檔名+頁碼或章節；XLSX 填「檔名 / 工作表名 / 列 N」（如「官網規格書.xlsx / 公司介紹 / 列 34」），嚴禁填指令編號
- "優先度"：[P0, P1, P2] 三選一，不加括號說明
- 不包含 Markdown 標記，不包含任何說明文字`;
